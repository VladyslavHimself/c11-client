type Props = {
    uploadedImage: null,
    setUploadedImage: () => null,
};

export function UploadImageFrame({ uploadedImage, setUploadedImage }: Props) {
    return (
        <div>
            upload image frame
        </div>
    );
}